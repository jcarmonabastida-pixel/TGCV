name: Reconstruct cumulative Evidence-to-Claim Matrix v1.15

on: push

permissions:
  contents: write

jobs:
  reconstruct:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate runner
        run: python3 -c "print('RECONSTRUCTION_WORKFLOW_STARTED')"
