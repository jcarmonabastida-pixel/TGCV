# EXT-1.1 — Rust

## Status

**GATED / NOT FROZEN.**

## Mandatory precondition

An identifiability/privacy audit must pass **before downloading or processing the candidate Rust dataset**.

Only after the audit passes:
1. identify the exact dataset;
2. record provenance and integrity metadata;
3. lock the experimental protocol/configuration;
4. execute the reproducible pipeline;
5. validate results;
6. create the immutable EXT-1.1 freeze.

## Data policy

No dataset is stored in this repository. Only manifests, provenance, hashes and reproducibility metadata belong here.

## Continuity note

This file is deliberately conservative: the repository currently records the recovered experimental boundary, not an invented frozen result.
