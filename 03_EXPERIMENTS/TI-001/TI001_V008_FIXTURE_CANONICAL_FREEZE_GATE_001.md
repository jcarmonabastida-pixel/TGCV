# TI-001 V008 Fixture Canonical Freeze Gate 001

**Status:** READY — FREEZE NOT AUTHORIZED

## Purpose

Establish the gate for promoting the integrity-validated V008 fixture from a generated local artifact to a canonically frozen experimental fixture.

## Preconditions

All of the following are required and currently satisfied:

1. V008 deterministic generation was explicitly authorized.
2. Generator/schema binding preflight passed.
3. Provider–contract compatibility preflight passed.
4. Fixture generation completed with `scientific_execution = NOT_PERFORMED`.
5. Fixture Integrity Preflight `TI001-V008-FIXTURE-INTEGRITY-PREFLIGHT-001` passed.
6. Fixture SHA-256 is `dd45ae453b5a3d37b4faff34ed829e7aac52ce4ed5a7462bb3546c1a420c2eb9`.
7. Generator blob SHA-1 is `f345c41371a189c43b69b707b49b7eb17d140f55`.
8. Schema blob SHA-1 is `d9539790452b047bc845a19bdcf50b8713a42b2a`.
9. Seed is `20260925`.
10. Scientific execution remains `NOT_PERFORMED`.

## Freeze scope

A future freeze authorization may bind only:

- fixture JSON bytes;
- fixture SHA-256;
- fixture ID;
- schema ID and schema blob SHA-1;
- generator ID and generator blob SHA-1;
- seed;
- generated integrity manifest;
- generation environment/timestamp metadata.

Freeze does **not** authorize:

- model/API execution;
- scientific execution;
- response collection;
- scoring or value calculation;
- interpretation;
- modification of the generator or schema;
- modification of the provider;
- modification of the execution contract.

## Canonicalization requirement

The fixture may be declared frozen only after its exact generated bytes and integrity manifest are committed to the canonical GitHub repository and the resulting committed artifact identity is recorded. A local generated file alone is not canonical governance state.

## Freeze authorization boundary

This gate does not itself authorize the freeze. A separate explicit authorization must be recorded after the generated artifacts are available for canonical registration.

## Scientific status

`scientific_execution = NOT_PERFORMED`.

No scientific evidence is created by this gate.
