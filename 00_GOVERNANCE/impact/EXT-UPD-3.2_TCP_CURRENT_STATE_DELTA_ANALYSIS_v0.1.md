# EXT-UPD-3.2 — TCP Current-State Delta Analysis v0.1

**Date:** 2026-09-08  
**Status:** CLOSED — CONTROLLED TCP DELTA ANALYSIS COMPLETE  
**Parent:** EXT-UPD-3 Controlled Drafting
**Asset:** `TGCV-EXT-TCP-001`

## 1. Historical reconstruction

The current canonical TCP is `05_ASSETS/TCP/TGCV-EXT-TCP-001_v0.2.md`. It is a historical working/conditional external artefact and must remain immutable.

Its empirical centre of gravity is the predictive comparison `M2` vs `M1` using an operational representation `R`. That framing is no longer the appropriate current centre of the programme after TR-131 and the closure of RUST-DYN-1/2.

## 2. Required current-state changes

### Retain
- `Core_ontological = S`;
- `T_acc = F(S,C,L)`;
- `ΔT_acc` as the central comparative phenomenon;
- distinction between accessibility and execution;
- downstream distinction among Reach, Trajectory, Outcome and Value;
- falsifiability and non-circular operationalization;
- cautious treatment of prior art and originality.

### Remove or supersede as current framing
- the TCP's predictive `R`-vs-baseline hypothesis as the principal research question;
- the claim that the immediate empirical objective is predictive incremental information of `R`;
- the obsolete `P(Y|S,B,R) != P(Y|S,B)` correction narrative as a current conceptual centre;
- any implication that Rust evidence established predictive performance.

### Add
- TR-131 consequence: `T_acc` is analytically indispensable but not an independent ontological primitive;
- RUST-DYN-1/2 as bounded structural/dynamic evidence;
- explicit `ΔT_acc` vs `ΔReach` distinction using ND-1/2/4;
- D-OPS-21 boundary: adaptation-space drift and related constructs reduce broad novelty;
- D-OPS-22 residual translational non-redundancy candidate;
- D-OPS-23 frozen minimal translation protocol;
- current open gates and non-claims;
- D-OPS-24 as the next controlled conformance test, without execution authorization.

## 3. Current contribution boundary

The TCP should now present TGCV primarily as a testable analytical architecture concerned with representing accessible transformation spaces and translating that structure across domains. It should not present the architecture as proven superior, original, causal, predictive of value or domain-independent.

## 4. Evidence boundary

Current empirical evidence supports only bounded E1 statements associated with the frozen Rust operationalization. It does not support causal, predictive, value-generating, universal, runtime-Cargo or full-originality claims.

## 5. Drafting consequence

A new TCP version is justified because the old TCP's central empirical framing is materially superseded. The new version must be additive and versioned; v0.2 remains immutable.

## 6. Next

Proceed to controlled drafting of `TGCV-EXT-TCP-001_v0.3.md`, then perform the same consistency and propagation chain used for RP v0.1.
