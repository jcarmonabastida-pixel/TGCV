# TGCV — VSL Synthetic Minimum v0.1
## Experimental Runner Preflight 001

**Status:** BLOCKED — CORRECTIVE REVISION REQUIRED  
**Runner:** `03_EXPERIMENTS/VSL_SYNTHETIC_MIN_V001/vsl_synthetic_min_runner_v01.py`  
**Runner commit:** `bc56b6ab63e558cd32958cfbc435a3a50b6df26f`  
**Frozen specification:** `VSL_SYNTHETIC_MIN_v0.1`

## 1. Preflight scope

Static audit of the runner before any experimental execution.

## 2. Checks

- Frozen baseline `S0=(10,10)`: PASS.
- Outcome path `final State -> O`: PASS.
- Value path `O -> V*`: PASS.
- Outcome function receives no accessibility/treatment/case input: PASS.
- T2 accessibility/value separation: PASS.
- T3 specified accessibility/selection pattern: PASS.
- NC2 accessibility-change/no-value-change pattern: PASS.
- T4 exogenous-control numerical pattern: PASS as output.
- T4 exogenous-factor implementation: **FAIL**.
- Experimental execution authorization: **BLOCKED**.

## 3. Blocking defect

The runner declares:

`exogenous_factor = 2`

and independently hard-codes:

`final_state = State(s0.q + 2, s0.r)`.

Therefore the factor is recorded but is not actually used to generate the final state.

This does not violate the outcome function's non-circularity, but it fails the stronger executable requirement that T4's downstream change be generated by an explicit exogenous mechanism rather than by a parallel hard-coded final state.

## 4. Required correction

Revise the runner so that T4 computes the final state from the exogenous factor, for example through an explicitly isolated exogenous transformation function. The outcome function must remain unchanged and must continue to receive only the final state.

After correction:

1. rerun this preflight;
2. verify the corrected source;
3. only then execute the runner;
4. record runtime output and hashes.

No experimental evidence, causal claim, or Evidence-to-Claim Matrix propagation is authorized by this preflight.

## 5. Governance disposition

**Runner construction:** complete but not accepted.  
**Runner preflight:** BLOCKED.  
**Experimental execution:** NOT AUTHORIZED.  
**Fixture:** remains PASS and frozen.  
**VSL specification:** unchanged.  
**Core/RMA/C09:** unchanged.
